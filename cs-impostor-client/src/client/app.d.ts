declare namespace Controller {
    interface State {
        leftButton: boolean;
        rightButton: boolean;
        mouseXDelta: number;
        mouseYDelta: number;
        mouseX: number;
        mouseY: number;
    }
}

declare namespace Game {
    interface Player {}
}
